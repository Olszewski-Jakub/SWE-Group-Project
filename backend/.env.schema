# Copy this file to `.env` and fill in values.
# `.env` is git-ignored; `.env.schema` is committable.

# JWT secret for HS256 signing/validation (>= 32 bytes). Example only — replace in your local .env
# Generate a strong Base64 value:  openssl rand -base64 32
JWT_SECRET=f28f6d50fb205746b4bc8a96656a1bcb0f8cf757e9856d929931ffc36987a233

# Database credentials
DB_USERNAME=demo
DB_PASSWORD=demo

# Optional: override DB URL (defaults to local Postgres in application.yml)
# DB_URL=jdbc:postgresql://localhost:5432/demo_db
